const SectionHeading = ({ subTitle, title, }) => {
    return (
        <div>
            <span>{subTitle}</span>

            <h2>{title}</h2>
        </div>
    );
};

export default SectionHeading;